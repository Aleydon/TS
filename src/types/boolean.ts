// boolean accepts (true, false)
const isAuthenticated: boolean = true;

console.log(isAuthenticated ? 'Authenticated' : 'Not authenticated');

const isAuthenticated2 = Boolean(0);
console.log(isAuthenticated2 ? 'Authenticated' : 'Not authenticated');
