// Null is a variable that contains no value.

const myUser: null = null;
const MyName: string | null = 'Aleydon';

console.log(myUser);
console.log(typeof myUser);

console.log(MyName);
console.log(typeof MyName);
