//  The tuple does not exist in javascript, only in typescript
//  Tuples must follow the standards for the type and number of values passed
//  Example: 2 values, the first number and the second string

const student: [number, string] = [2024, 'Aleydon'];
const orderStatus: [number, string] = [200, 'OK'];

console.log(student);
console.log(orderStatus);
